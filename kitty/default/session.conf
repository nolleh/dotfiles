# workspace1
new_tab
enabled_layouts tall:bias=68;mirrored=true, stack
launch --cwd $WS1 zsh
launch --title demonruns --cwd $WS1 zsh
launch --title exec --location=hsplit --cwd $WS1 zsh
resize_window taller 12

# workspace2
new_tab
enabled_layouts tall:bias=68;mirrored=true, stack
launch --cwd $WS2 zsh
launch --title demonruns --cwd $WS2 zsh
launch --title exec --location=hsplit --cwd $WS2 zsh
resize_window taller 12

#create new tab
new_tab scratch
enabled_layouts tall,fat,stack
layout fat
launch --cwd $WS3 zsh
launch --title exec --cwd $WS3 zsh
resize_window shorter 5

# create new tab
new_tab hugo/devops
enabled_layouts tall,fat,stack
layout fat
launch --cwd $WS4 zsh
launch --title exec --cwd $WS4 zsh
resize_window shorter 5

# create new tab
new_tab
enabled_layouts tall,fat,stack
layout fat
launch --cwd $WS5 zsh
launch --title exec --cwd $WS5 zsh
resize_window shorter 5
