new_tab 
# create window
launch zsh

# create window
launch --title demonruns zsh
launch --title exec --location=hsplit zsh
resize_window taller 15 

#create new tab
new_tab scratch
enabled_layouts tall,fat,stack
layout fat
launch zsh
launch --title exec zsh
resize_window shorter 13

# create new tab
new_tab hugo
enabled_layouts tall,fat,stack
layout fat
launch zsh
launch --title exec zsh
resize_window shorter 13
