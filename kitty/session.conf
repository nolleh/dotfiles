# workspace1
new_tab 
enabled_layouts tall:bias=68;mirrored=true, stack
launch --cwd $WORKSPACE zsh
launch --title demonruns --cwd $WORKSPACE zsh
launch --title exec --location=hsplit --cwd $WORKSPACE zsh
resize_window taller 12

# workspace2
new_tab 
enabled_layouts tall:bias=68;mirrored=true, stack
launch --cwd $WORKSPACE zsh
launch --title demonruns --cwd $WORKSPACE zsh
launch --title exec --location=hsplit --cwd $WORKSPACE zsh
resize_window taller 12

#create new tab
new_tab scratch
enabled_layouts tall,fat,stack
layout fat
launch zsh
launch --title exec zsh
resize_window shorter 5

# create new tab
new_tab hugo/devops
enabled_layouts tall,fat,stack
layout fat
launch zsh
launch --title exec zsh
resize_window shorter 5

# create new tab
new_tab 
enabled_layouts tall,fat,stack
layout fat
launch zsh
launch --title exec zsh
resize_window shorter 5
